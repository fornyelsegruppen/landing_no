# ONE UI owner UAT — durable issue log and retest checklist

Date: 2026-09-05
Scope: protected Preview and isolated Preview data only. Production, stable RF Preview, aliases, DNS and provider-account configuration remain out of scope.

## Controlled references

| Reference                    | Exact value                                                                     | Control                                                                                  |
| ---------------------------- | ------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------- |
| Frozen owner baseline        | `d10de7b2749a8ea0c33cf90037896f66522736e9` / `dpl_F3HDePZ7GPcAhADNaqwG7bjy8K5p` | Historical evidence only; do not retest as the candidate.                                |
| Current stable candidate     | `74fb68768708949619c31acdf8532f92b0e93326` / `dpl_HZ5C9TWbAsg3rPmxtGd81NmPbHjs` | Keep stable while the next candidate is prepared.                                        |
| Current stable candidate URL | `https://landing-hdl10bjlj-darbasnorvegija4-8212s-projects.vercel.app`          | Owner-authenticated Preview smoke passed; do not move aliases or mutate this deployment. |
| Next candidate               | Not deployed yet                                                                | Must receive its own SHA, deployment ID, immutable URL and evidence entries before UAT.  |

Status vocabulary:

- **VERIFIED RESOLVED** — observed on the exact deployed candidate.
- **IMPLEMENTED LOCALLY — RETEST REQUIRED** — code exists in the working candidate but has not yet been verified on its own immutable deployment.
- **OPEN** — desired behavior is documented but is not yet claimed as implemented.

## Evidence ledger

Evidence and diagnosis are deliberately separate. A log-derived fact is not replaced by a suspected explanation, and stored message status is not treated as provider-delivery proof.

| Evidence ID               | Exact observation                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Source and durability                                                                                                                                                           |
| ------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| E-504-01                  | On the frozen deployment, `/admin/login` and `/admin-next-preview/cases/1` ended in authenticated 504 responses after 60 or 300 seconds. Runtime output stopped at Payload's interactive development-schema-push prompt.                                                                                                                                                                                                                                                                                                                        | Frozen Vercel runtime logs; confirmed observation.                                                                                                                              |
| E-504-02                  | The isolated candidate database contained all 44 filesystem migrations and a `payload_migrations` row named `dev` with batch `-1`. The candidate build logged `Cleared drizzle-push marker(s): dev` and then `Migrations complete.`                                                                                                                                                                                                                                                                                                             | Isolated Preview DB read and candidate build log; confirmed observation.                                                                                                        |
| D-504-01                  | The stale Payload development migration marker caused the runtime migration path to wait for interactive input and therefore caused the frozen 504.                                                                                                                                                                                                                                                                                                                                                                                             | High-confidence root-cause conclusion supported by E-504-01 and E-504-02; not a separate screenshot assertion.                                                                  |
| E-CAND-AUTH-01            | In the owner's Vercel-authenticated Chrome session, candidate `/admin/login` rendered in 8.122 seconds on the cold check and synthetic administrator login succeeded.                                                                                                                                                                                                                                                                                                                                                                           | Owner session plus Vercel request evidence; candidate-specific.                                                                                                                 |
| E-CAND-NAV-01             | The canonical work queue and TF-1 workspace rendered in about 1.3 seconds; the work queue survived a full reload in 2.503 seconds.                                                                                                                                                                                                                                                                                                                                                                                                              | Owner-authenticated candidate navigation; candidate-specific.                                                                                                                   |
| E-CAND-HTTP-01            | Vercel recorded HTTP 200 for `/admin/login`, `/admin-next-preview/work`, `/admin-next-preview/cases/TF-1`, `/api/users/me` and the communications pagination request. No candidate 5xx was observed during this smoke.                                                                                                                                                                                                                                                                                                                          | Vercel request logs; confirmed only for the observed window.                                                                                                                    |
| E-CAND-HIST-01            | TF-1 showed customer-question focus, 25 of 27 messages, commercial/document sections and the business timeline. Loading older messages produced 27 of 27 and moved focus to the completion status.                                                                                                                                                                                                                                                                                                                                              | Owner-authenticated candidate observation. It proves bounded loading, not that the default information hierarchy is acceptable.                                                 |
| E-PUBLIC-01               | Public `/no` and the two-step contact form were opened without submission.                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Read-only owner observation. No lead, customer email or provider delivery was created.                                                                                          |
| E-PUBLIC-ADDRESS-01       | On step 2 of the current stable candidate's `/no#kontakt` form, typing `lyngveien` in **Adresse (valgfritt)** left a plain text input. No suggestion list, loading state, no-results state or provider-error state appeared.                                                                                                                                                                                                                                                                                                                    | Owner screenshot and direct observation on the current stable candidate. This proves the visible symptom only; it does not identify the code or provider cause.                 |
| E-PUBLIC-ADDRESS-DIAG-01  | The public contact component stores the address as a plain string and updates it directly on input. `autoComplete="street-address"` is only a browser-autofill hint. The submit payload and public lead API carry address text only: there is no suggestion/candidate identity, coordinate payload or stale-selection invalidation. The existing Kartverket provider is wired to Admin/RF and automatic-package paths, not to the public lead form.                                                                                             | Confirmed source-code diagnosis plus 7/7 passing related diagnostic tests. This is diagnostic evidence, not implementation or deployed visual proof.                            |
| E-LOCAL-VERIFY-01         | Focused verification passed 4 files / 66 tests; the Admin Next suite passed 57 files / 476 tests; TypeScript passed; lint and the semantic-token gate passed with only the existing `admin-next-r4-measurement-review.tsx` `<img>` warning; `git diff --check` passed after this documentation update.                                                                                                                                                                                                                                          | Local code verification only. This is not deployment, browser-layout or owner UAT evidence.                                                                                     |
| E-INTAKE-02               | The owner submitted the controlled form on candidate `hdl10bjlj` at `2026-09-05T06:47:59Z`; the browser reached `/no/takk`, and `POST /api/lead` returned HTTP 200.                                                                                                                                                                                                                                                                                                                                                                             | Exact candidate owner observation plus runtime request evidence. This proves request acceptance, not downstream email delivery or data correctness.                             |
| E-CASE-02                 | The persisted record is lead ID `2` / case `TF-2`: name `TEST – pilnas kelias`, email `fornyelsegruppen@gmail.com`, phone `fornyelsegruppen@gmail.com`, address `lyngveien, 28`, postal `1182`, city `null`, `approxSqm` `300`, service `takvask`, status `new`, consent granted and marketing consent granted. The free-text message says the exact area is unknown.                                                                                                                                                                           | Read-only isolated Preview record inspection. The values are recorded facts, not verified identity, phone, address, area or measurement evidence.                               |
| E-RECEIPT-28              | Message ID `28` is a non-AI receipt (`aiAssisted=false`) addressed to the exact owner Gmail with `[PREVIEW TEST]`. It remains `queued`; `sentAt`, `provider=resend` and `providerMessageId` are present, while `deliveredAt` is null and `failureCode=Error` plus `failureMessage` are present. The receipt-outbox path captured an exception with the owner-supplied truncated correlation `5a206…`.                                                                                                                                           | Read-only message/runtime evidence. Classification is **provider accepted / application state ambiguous**. It proves neither delivery nor non-delivery. No retry is authorised. |
| E-JOB-01                  | Operational job ID `1` for `message.delivery` is `pending` with `attempts=0`. No AI/Gemini job, AI draft or AI message exists for TF-2; only the manual preparation control is present.                                                                                                                                                                                                                                                                                                                                                         | Read-only isolated Preview job and case inspection. Pending is not execution evidence, and no retry/run is authorised by this log.                                              |
| E-ONE-UI-02               | `/admin-next-preview/cases/TF-2` shows one queued message and zero quotes, contracts, documents and questions. Its next action is read-only; no yellow prepare action is available.                                                                                                                                                                                                                                                                                                                                                             | Owner-authenticated read-only ONE UI observation on the exact candidate.                                                                                                        |
| E-ONE-UI-DELIVERY-02      | The expanded TF-2 receipt shows a queued badge and queued delivery state while its footer simultaneously says sent at `2026-09-05 08:48`.                                                                                                                                                                                                                                                                                                                                                                                                       | Owner screenshot/observation on the exact candidate. This is a contradictory application display, not proof of provider delivery.                                               |
| E-INBOX-02                | The owner inbox received two separate-purpose messages: the customer receipt `[PREVIEW TEST] Vi har mottatt henvendelsen din` and the internal notification `[PREVIEW TEST] Ny henvendelse: TEST – pilnas kelias (Takvask)`. The latter was from `Takfornyelse <post@takfornyelse.as>` to `fornyelsegruppen@gmail.com`, reply-to the same address, dated Sep 5 2026 9:48 AM.                                                                                                                                                                    | Owner inbox/header observation. These are not duplicate receipts. Inbox presence does not reconcile message `28`'s stored/provider fields and does not authorise a retry.       |
| E-INTERNAL-NOTIFY-CODE-02 | `src/app/api/lead/route.ts:405-438` independently sends an internal lead notification after the receipt path, to `LEAD_TO_EMAIL \|\| siteConfig.email`, with a generated PDF attachment. This direct Resend send is not persisted as a case communication or provider message ID.                                                                                                                                                                                                                                                               | Confirmed source-code diagnosis. It explains the second email's distinct purpose but is not provider-delivery evidence.                                                         |
| E-INTERNAL-PDF-02         | Local file `C:\Users\Fornyelsegruppen\Downloads\henvendelse-2-test-pilnas-kelias.pdf` is a one-page A4 PDF with title `Henvendelse 2`, subject `Kundens henvendelse til Takfornyelse`, creator `Takfornyelse dokumentplattform` and SHA-256 `633D07E8B5CA62DC2B52A8781A11F5942AE3975E9BD926BED7DD89CBBFDEDA6B`. Rendered text contains `Ny henvendelse`, `Sak #2`, `Takvask`, the submitted customer intake, `300` m², zero images, landing URL and protected gallery/admin links; it contains no quote, price, contract or AI text.            | Owner-provided local artifact metadata/render/text evidence. The PDF itself has no visible Preview/internal watermark and is not yet a repository release artifact.             |
| E-ADMIN-LANGUAGE-DIAG-02  | A persisted `users.interfaceLanguage` preference exists (`nb`, `lt`, `en`, default `nb`), but the direct internal notification currently resolves only an email recipient and reuses the customer-locale payload for subject/body/PDF.                                                                                                                                                                                                                                                                                                          | Confirmed source-code diagnosis. Admin-recipient language selection is not yet implemented or deployed.                                                                         |
| E-LOCAL-UX-02             | The bounded Admin Next identity/intake/delivery patch passed 8 focused files / 234 tests, TypeScript and lint/semantic verification; lint retained only the existing `admin-next-r4-measurement-review.tsx` `<img>` warning.                                                                                                                                                                                                                                                                                                                    | Local code verification only. No deployment, browser screenshot, DB write, provider send or owner UAT is claimed.                                                               |
| E-DB-BRANCH-CONFIG-03     | Vercel Native Neon resource `takfornyelse-one-ui-preview-0de4761-db` is connected only to project `takfornyelse-one-ui-preview-0de4761` for Preview. The connection was saved and reopened showing `Require Active Resource Before Deploy = Required`, Preview deployment branching enabled, Production disabled and Sensitive disabled. During the first ambiguous UI interaction Sensitive was momentarily saved on, which was stricter than the original setting; it was restored to the original off state before the final save/read-back. | Owner-authorized isolated Preview connection mutation plus immediate read-back in Vercel UI. Production, the stable deployment and TF-2 were not changed.                       |
| E-DB-MIGRATE-RECOVERY-03  | Manual Preview-only `DATABASE_URL_MIGRATE` is Sensitive and cannot be exported or reconstructed by Vercel. It was therefore retained. The next deployment must pass an empty build-only override so `run-migrate.mjs` uses the per-deployment integration `DATABASE_URL`; a fail-closed guard requires PostgreSQL, a canonical Neon endpoint distinct from the protected base endpoint and the exact approved Neon project ID.                                                                                                                  | Recovery-preserving release control. The deployment and database migration have not yet run.                                                                                    |
| E-DB-DEPLOY-GATE-03       | The isolated-project Preview deploy command must pass an empty build-only `DATABASE_URL_MIGRATE`, protected base endpoint SHA-256 `c09b85f7a4792458554f96177d4caba345a0cebeb3efcc03d54954fe851a314e` and expected Neon project ID `ancient-paper-21872391`. These are one-deployment build overrides: they do not delete or replace the retained Project Preview secret. Build and authenticated runtime fingerprints must match before the candidate is accepted.                                                                              | Independent release-agent conditional GO and 16/16 guard/security tests. Actual deployment remains pending.                                                                     |
| E-AI-CONFIG-03            | Preview configuration reports `FEATURE_AI_DRAFTS=false`, no declared `GEMINI_API_KEY`, and `AUTOMATION_EMERGENCY_PAUSE=true`. The UI presents manual drafting as available and AI as unavailable without inventing provider provenance.                                                                                                                                                                                                                                                                                                         | Read-only configuration evidence. No AI provider key, feature flag or emergency-pause setting was changed.                                                                      |
| E-SECURITY-03             | Reply preparation and delivery boundaries now include one active draft per question, exact canonical reply approval, in-flight delivery retry rejection, terminal-case rejection, normalized manual-placeholder rejection, permission separation and CAS checks.                                                                                                                                                                                                                                                                                | Local implementation plus independent security-agent GO; deployment retest remains required.                                                                                    |
| E-LOCAL-GO-03             | Full unit regression passed 370 files with 2,151 tests, with one file and three tests intentionally skipped. Migration regression passed 26 files / 48 tests. TypeScript, lint/semantic tokens and `git diff --check` passed; lint retained only the pre-existing RF `<img>` warning.                                                                                                                                                                                                                                                           | Local verification only. No deployment, runtime DB mutation, customer send or owner UAT is claimed.                                                                             |

The following session-local screenshots preserve the original owner observations but are not durable release artifacts. A next-candidate pass must copy replacement images into a repository evidence directory and record SHA-256 hashes.

| Evidence ID | Observation                                                 | Session-local source                                                                            |
| ----------- | ----------------------------------------------------------- | ----------------------------------------------------------------------------------------------- |
| E-SCREEN-01 | Desktop arrival and expanded filters                        | `C:\Users\FORNYE~1\AppData\Local\Temp\codex-clipboard-99c92bd5-73dd-463c-947f-77719f3bdde1.png` |
| E-SCREEN-02 | Selected detail and denied action                           | `C:\Users\FORNYE~1\AppData\Local\Temp\codex-clipboard-a1adeeb0-acec-42b1-99a8-c6e571ece8e2.png` |
| E-SCREEN-03 | Narrow viewport repeated card/detail                        | `C:\Users\FORNYE~1\AppData\Local\Temp\codex-clipboard-8b76ee98-d671-458f-b4a4-4bc7f64c69a7.png` |
| E-SCREEN-04 | Case header, process position and deadline                  | `C:\Users\FORNYE~1\AppData\Local\Temp\codex-clipboard-8a4e4bc7-0236-4998-a987-281a2d184a23.png` |
| E-SCREEN-05 | Bounded dialogue list and legacy deep links                 | `C:\Users\FORNYE~1\AppData\Local\Temp\codex-clipboard-0782185e-fe30-4609-be48-53f60e67bc1a.png` |
| E-SCREEN-06 | Sticky-tab overlap, older-message control and legacy footer | `C:\Users\FORNYE~1\AppData\Local\Temp\codex-clipboard-3cb40f42-67c5-46ed-8084-9084cd642d59.png` |

## UAT-01 — frozen authenticated 504 and candidate recovery

- **Severity:** P1.
- **Status:** **VERIFIED RESOLVED** on the current stable `hdl10bjlj` candidate; repeat on the next candidate.
- **Reproduction:** on the frozen deployment, authenticate and open `/admin/login` or `/admin-next-preview/cases/1`; compare with the same authenticated routes on the current stable candidate.
- **Actual:** the frozen routes waited on Payload's development schema prompt and returned 504. The current stable candidate returned the login, work queue, TF-1 case and reload successfully.
- **Expected:** a deployed Preview must never invoke or wait on an interactive schema-push decision. Login, work queue, case workspace and reload must return without 5xx.
- **Evidence:** E-504-01, E-504-02, E-CAND-AUTH-01, E-CAND-NAV-01 and E-CAND-HTTP-01.
- **Cause classification:** D-504-01 is supported by runtime, DB and build evidence. Do not substitute a generic “login timeout” label for this exact frozen failure.
- **Next-candidate check:** capture the first cold `/admin/login`, authenticated work queue, TF-1 case and full reload timings; retain corresponding request-log status codes.

## UAT-02 — “Show details” versus full-case navigation

- **Severity:** P2.
- **Status:** **IMPLEMENTED LOCALLY — RETEST REQUIRED** on the next immutable candidate.
- **Reproduction:** open `/admin-next-preview/work?view=today&queue=all&limit=25`, select TF-1 with **Show details / Rodyti detales**, follow the full-case action, then return.
- **Actual owner observation:** the in-page selection used `selected=case%3A1#work-queue-detail`, repeated much of the card and did not clearly distinguish task detail from the full case. A denied command could also hide the separately permitted read-only case workspace.
- **Expected:** the two actions have distinct labels and outcomes:
  - **Show details** changes only the in-page selected task, moves focus to a stable detail heading and preserves the selected anchor;
  - **Open case** navigates to the exact ONE UI case workspace without granting the denied mutation capability;
  - **Back to work queue** restores view, queue, filters, selected case and a meaningful focus target;
  - an unavailable command never removes an independently allowed `case.read` route.
- **Evidence:** E-SCREEN-02 and E-SCREEN-03 are the original observations. The next candidate needs URL-before/after, focus-target and restored-state screenshots at desktop and mobile widths.
- **Suspected cause:** none is claimed. The issue is an information-architecture and focus-management ambiguity, not evidence of a permissions backend failure.

## UAT-03 — dialogue history buries commercial and document work

- **Severity:** P2.
- **Status:** **IMPLEMENTED LOCALLY — RETEST REQUIRED** on the next immutable candidate. The current stable deployment still proves only the earlier 25/27 pagination path in E-CAND-HIST-01.
- **Reproduction:** open TF-1 and inspect the dialogue panel before pressing **Show older**; compare the location of offers, contracts and documents before and after expanding all 27 messages.
- **Actual owner observation:** the default case dialogue renders 25 of 27 entries. Even though the list is bounded, it dominates the case and pushes offers, contracts and documents down the page. Loading 27 of 27 proves the data path but does not solve the hierarchy.
- **Exact local behavior awaiting deployment and visual retest:**
  - the default recent view renders at most the five newest loaded messages;
  - the explicit full-history view is addressable by `#case-communications-history`, so it can be opened and revisited without replacing the case workspace;
  - full history provides text search plus category and status filters over the messages currently loaded in the browser; it does not claim server-wide search over pages that have not been loaded;
  - **Show older** remains an explicit bounded pagination action and extends the loaded set rather than creating an endless-scroll trigger;
  - every outstanding customer question is rendered in a separate outstanding-questions list, including the oldest unanswered question when it falls outside the five-message recent slice;
  - offers, contracts and documents retain their own case areas instead of being represented only through message history;
  - message records expand in the document flow and do not intentionally create a nested page-sized scrollbar.
- **Required next-candidate proof:** verify the five-item limit, hash switching, loaded-only search/filter semantics, older pagination, complete outstanding-question preservation and scroll behavior with the 0/1/27/100 matrix below.
- **Evidence:** E-LOCAL-VERIFY-01 is local automated verification only. E-CAND-HIST-01, E-SCREEN-05 and E-SCREEN-06 remain the deployed/owner evidence for the prior behavior. Stored `sent` or `delivered` labels are history data, not external provider proof.

## UAT-04 — read-only, Preview-test and executable states

- **Severity:** P2.
- **Status:** **IMPLEMENTED LOCALLY — RETEST REQUIRED** on the next immutable candidate.
- **Reproduction:** compare one case with `case.read` only, one Preview-gated test action, one executable action, and one missing-data prerequisite.
- **Actual owner observation:** operator copy mixed raw terms such as `F2`, `canonical`, `case:1`, revisions, raw status/category values and capability denial. It could be read as if the whole workspace were read-only or as if stored delivery state proved a real send.
- **Expected:** every primary action shows one consistent state:
  - **Read-only:** the record can be inspected, but the required capability is absent;
  - **Preview test:** an explicitly gated, non-Production action with its test limitation visible;
  - **Executable:** the exact allowed action and target are available;
  - **Data prerequisite:** the capability may exist, but required case data is missing.
- The current stage, completed/known pending stages, one next action and its reason appear before technical diagnostics. Human LT/NB/EN labels are primary; raw IDs, revisions, stored enums and capability names remain collapsed. UI projection never replaces server-side permission checks.
- **Evidence:** E-SCREEN-02, E-SCREEN-04 and E-SCREEN-05. The next candidate must capture all four states and record role, enabled flags and whether an action was merely opened or actually executed.

## UAT-05 — sticky navigation overlap and scroll ownership

- **Severity:** P2.
- **Status:** **IMPLEMENTED LOCALLY — RETEST REQUIRED** on the next immutable candidate.
- **Reproduction:** at 360/375, 768 and desktop widths, scroll through the case header, sticky panel tabs and expanded message content; use keyboard tab/arrow navigation and load older messages.
- **Actual owner observation:** sticky case tabs overlapped message content and the message region introduced a nested scroll trap inside the page.
- **Expected:** the document owns vertical scrolling; message bodies and history do not create a second page-height scroll container. Sticky header/tab offsets never overlap content, focused controls remain visible, and reduced-motion/keyboard navigation retains a stable focus destination after selection and pagination.
- **Evidence:** E-SCREEN-03 and E-SCREEN-06. The next candidate needs top/middle/bottom screenshots and a short focus/scroll observation at every required width.

## UAT-06 — public address lookup has no visible suggestion states

- **Severity:** P1 for RF-ready intake integrity; P2 for completing a contact-only intake manually.
- **Status:** **IMPLEMENTED LOCALLY — RETEST REQUIRED** on the next immutable candidate. No address-suggestion capability is claimed for the current stable candidate.
- **Reproduction:** open the current stable candidate at `/no#kontakt`, continue to step 2, focus **Adresse (valgfritt)** and type `lyngveien`.
- **Actual owner observation:** the field behaves as a plain text input. There is no Norwegian address suggestion list and no visible loading, no-results or provider-error state.
- **Evidence:** E-PUBLIC-ADDRESS-01. The owner screenshot/observation establishes only what was visible in the tested browser session.
- **Confirmed diagnosis:** autocomplete is absent from the current public-form implementation; this is not a provider or network failure. The public component uses plain string state and direct `onChange`; `autoComplete="street-address"` requests browser autofill only. Submission and the lead API preserve text only, without a candidate ID, coordinates or stale-selection invalidation. Kartverket lookup exists elsewhere but is connected to Admin/RF and automatic-package flows rather than `/no#kontakt`.
- **Diagnostic evidence:** E-PUBLIC-ADDRESS-DIAG-01 and its 7/7 passing diagnostic tests. Keep this separate from E-PUBLIC-ADDRESS-01, which remains the owner's browser observation.
- **Exact local behavior awaiting deployment:**
  - Norwegian suggestions include street, house number, postcode and place, with unambiguous labels;
  - suggestions support keyboard navigation, Enter/Escape behavior and touch selection without trapping focus;
  - loading, no-results and provider-error states are visible and accessible, while a clearly labelled manual-text fallback remains available;
  - selecting a suggestion preserves the canonical formatted address and coordinates through public lead creation, case projection and the RF handoff;
  - editing the address text after selection invalidates the stale canonical selection and coordinates;
  - a manual address may be stored for contact follow-up but must not unlock RF measurement or commercial calculation until the address/coordinates are verified;
  - no client-supplied coordinate or stale suggestion state is silently promoted to verified measurement evidence.

### UAT-06 next-candidate retest cases

| Case                 | Action                                                                                                             | Expected evidence                                                                                                                                 |
| -------------------- | ------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| Short query          | Enter fewer than the documented minimum characters, pause, then continue typing                                    | No premature provider claim; helper text makes the threshold clear; loading begins only when the query is eligible.                               |
| No results           | Enter a controlled query known to return no Norwegian address                                                      | Explicit no-results state; typed text is retained; manual fallback remains available and is not represented as verified.                          |
| Provider error       | Exercise a controlled failed/timeout response                                                                      | Distinct error state with safe retry and manual fallback; no empty-success or verified-coordinate state.                                          |
| Selection            | Choose a result containing street, house number, postcode and place                                                | Visible selected state; canonical address and coordinates are submitted once and project unchanged into the lead/case.                            |
| Edit after selection | Select a result, then change any address character                                                                 | Selected identity and coordinates are invalidated immediately; RF/measurement remains locked until a new verified selection.                      |
| Keyboard and mobile  | Navigate results with keyboard, Enter/Escape and touch at 360/375, 768 and desktop widths                          | Focus order, active option, dismissal and selection are perceivable; list is not clipped by sticky UI or the viewport.                            |
| Case/RF projection   | Complete a controlled synthetic intake after explicit owner approval, then inspect the resulting case and RF entry | Canonical address and coordinates match the selected result; source/verification state is explicit; manual-only address cannot start measurement. |

## UAT-07 — work-queue rows lack compact case identity

- **Severity:** P2.
- **Status:** **IMPLEMENTED LOCALLY — RETEST REQUIRED** on the next immutable candidate. The post-submit owner observation remains the current stable candidate's baseline.
- **Reproduction:** open `/admin-next-preview/work?view=today&queue=all&limit=25` after the controlled intake and compare the TF-1 and TF-2 rows before opening either detail.
- **Actual owner observation:** the rows omit customer name, address and received time. TF-1 and TF-2 are therefore nearly indistinguishable without opening details, even though TF-2 is the newly submitted controlled case.
- **Expected:** each compact row presents the human case reference, customer identity, concise address and received/created time before technical state. Current stage, status and next action remain visible, but raw IDs and diagnostics do not substitute for identity. Missing values are labelled as missing rather than silently omitted.
- **Exact local behavior awaiting deployment:** each row now uses the canonical work item to show a single compact identity line with customer name, truncated address and received timestamp. The selected detail repeats the same received timestamp alongside case/customer/address context. Selection stays URL-backed and the existing queue/stage/action/owner filters and server permissions are unchanged.
- **Evidence:** E-INTAKE-02, E-CASE-02, E-LOCAL-UX-02 and the owner queue observation. A next-candidate screenshot must include TF-1 and TF-2 together at mobile and desktop widths without exposing more personal data than required for this synthetic test; confirm truncation, filter results and selected-detail parity.

## UAT-08 — intake validation and RF integrity

- **Severity:** P1 because unverified or contradictory intake values must not become measurement, price or schedule evidence.
- **Status:** **IMPLEMENTED LOCALLY — RETEST REQUIRED** on the next immutable candidate. TF-2 remains frozen in the stable Preview; no correction or RF promotion is authorised by this entry.
- **Reproduction:** inspect persisted lead ID `2` / case `TF-2` after the `2026-09-05T06:47:59Z` submission, then compare the form values, free-text message, ONE UI case and any RF eligibility.
- **Actual:**
  - the phone field accepted the email address `fornyelsegruppen@gmail.com`;
  - the free-text address is stored as `lyngveien, 28`, with postal `1182`, city `null` and no verified canonical selection or coordinates;
  - `approxSqm=300` is stored while the message states that the exact area is unknown;
  - the case is `new`, and the ONE UI next action is read-only with no yellow prepare action.
- **Expected:**
  - phone validation rejects email-shaped input and preserves phone and email as distinct typed fields;
  - address, postcode and place are normalized or explicitly marked manual/unverified; city is not silently missing when a canonical selection exists;
  - area input and free-text uncertainty do not create conflicting assertions: an approximate customer value remains labelled approximate and never becomes measured area;
  - unverified address, missing coordinates and approximate/conflicting area cannot unlock RF measurement, pricing, duration or scheduling;
  - RF promotion requires a separately verified canonical address and coordinates, followed by the normal measurement evidence and approval gates;
  - consent and marketing-consent records remain auditable but do not validate the unrelated phone, address or area fields.
- **Read-only acceptance check:** E-ONE-UI-02 correctly shows one queued message and zero quotes, contracts, documents and questions. The absence of a yellow prepare action is the safe current result.
- **Exact local behavior awaiting retest:** phone and email are validated as distinct typed fields; a selected Kartverket candidate persists canonical street/postcode/place, provider ID and coordinates; editing the text clears stale verification. Manual or failed address lookup remains contact-only. RF, Norge i Bilder, measurement proposal and commercial preparation require verified provider provenance and coordinates. Customer-reported approximate area remains explicitly unverified and cannot substitute for measurement evidence. The `prepare_package` presentation states that address and measurement basis must be verified before price, quote or contract preparation.
- **Evidence:** E-CASE-02 and E-ONE-UI-02. The next candidate must test invalid phone, manual address, canonical address selection, missing city, approximate/unknown area and RF gate behavior independently.

## UAT-09 — receipt provider acceptance and application state diverged

- **Severity:** P1 for duplicate-send prevention and truthful communication history.
- **Status:** **OPEN — READ-ONLY INVESTIGATION REQUIRED**. No retry, job run, status repair or provider action is authorised.
- **Reproduction:** inspect message ID `28`, operational job ID `1` and runtime exception evidence for the controlled TF-2 submission without invoking delivery.
- **Actual:** message ID `28` contains a provider and provider message ID plus `sentAt`, but application status is `queued`, `deliveredAt` is null and failure fields are populated. The receipt-outbox path captured an exception; job ID `1` remains pending with zero attempts.
- **Actual ONE UI contradiction:** the expanded receipt currently combines a queued badge/journey with a footer labelled sent at `2026-09-05 08:48`. The owner inbox contains the customer receipt, but that separate inbox fact does not repair or explain the stored mixed state.
- **Classification:** **provider accepted / application state ambiguous**. Do not describe the receipt as delivered, and do not describe it as unsent. Provider acceptance and inbox delivery are different facts; the current application fields are internally divergent.
- **Expected:** a provider-accepted send is durably reconciled without scheduling an unsafe duplicate. History separately represents provider acceptance, application finalization, confirmed delivery and failure/recovery. A retry requires an idempotency-safe read of provider/application state plus explicit authorization.
- **Exact local presentation awaiting deployment:** any nonterminal stored status with either `sentAt` or a provider message ID is projected as `provider_accepted_unreconciled`, including either evidence field on its own. ONE UI shows **status unclear**, preserves the available provider message ID and recorded `sentAt`, labels that timestamp as not being delivery confirmation, and instructs the operator to reconcile the provider log before any resend. The sent stage is not styled as completed/success.
- **AI boundary:** no AI/Gemini job, AI draft or AI message exists for TF-2, and `aiAssisted=false` on message `28`. The visible preparation option is manual only.
- **Evidence:** E-RECEIPT-28, E-JOB-01, E-ONE-UI-DELIVERY-02, E-INBOX-02 and E-LOCAL-UX-02. Preserve the provider message ID and correlation for diagnosis; do not expose secrets or execute the pending job during evidence collection.

## UAT-10 — internal notification role, history and language are unclear

- **Severity:** P1 for operator/customer role clarity and safe audit history; P2 for localization quality.
- **Status:** **PARTIALLY IMPLEMENTED LOCALLY — RETEST REQUIRED**. Localization, role marking and PDF watermarking are implemented locally; durable internal-notification/provider-result history remains open and must not be claimed complete.
- **Reproduction:** submit one owner-authorized synthetic Preview inquiry, compare the customer receipt with the direct internal lead notification, open the attached PDF, then inspect the ONE UI message history.
- **Actual:** two distinct-purpose messages reach the same controlled inbox without strong enough role separation. The internal subject has `[PREVIEW TEST]`, but the attached PDF itself does not visibly say internal/Preview test. ONE UI records the customer receipt only; the direct internal notification and its provider result are outside the case communication history. The generic receipt remains `aiAssisted=false` and is not a substantive customer response.
- **Expected:** the admin notification subject, body and PDF are visibly marked **INTERNAL / PREVIEW TEST**; the PDF watermark survives rendering and forwarding. The internal notification has an auditable case-history record distinct from customer communication, without exposing an internal-only artifact to the customer. Customer receipt and admin notification remain different message roles rather than being presented as duplicates.
- **Admin-language requirement:** internal subject/body and generated PDF headings/labels use each recipient administrator's saved `users.interfaceLanguage`; the original customer text remains verbatim. Customer receipt remains in the customer's form language. The explicit documented fallback applies only when a recipient has no valid saved preference. Multiple recipients are localized per recipient rather than using the first recipient's language for all.
- **Exact local behavior awaiting deployment:** recipient administrators are resolved with their persisted interface language; LT/NB/EN internal mail and one-page PDFs carry explicit INTERNAL/PREVIEW marking, preserve the submitted customer text and use Unicode-safe embedded fonts. The protected language selector writes the authenticated database preference, and server rendering reads that preference instead of a client cookie.
- **Still open after this batch:** the direct internal notification and its provider result are not yet a first-class private timeline/message record; inbound Resend attachments and long bodies need separate durable ingestion hardening; agreement/document version chains need uncapped auditable registries. These findings are retained as release backlog rather than hidden by the localization fix.
- **Evidence:** E-INBOX-02, E-INTERNAL-NOTIFY-CODE-02, E-INTERNAL-PDF-02 and E-ADMIN-LANGUAGE-DIAG-02. Preserve Preview subject branding and exact-recipient allowlist behavior; do not resend for testing without separate authorization.

### UAT-10 localization retest matrix

| Case                   | Setup                                                                                                           | Expected evidence                                                                                                                                 |
| ---------------------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------- |
| LT admin + NO customer | Recipient admin saved as `lt`; customer submits `/no`                                                           | Internal subject/body/PDF labels are LT; customer receipt is NO; original NO customer text is byte-for-byte equivalent after PDF/text extraction. |
| NO admin + NO customer | Recipient admin saved as `nb`; customer submits `/no`                                                           | Both messages may be NO, but INTERNAL versus customer-receipt role is unmistakable in subject/body/PDF and history.                               |
| Changed preference     | Save recipient as one locale, send a controlled case, change preference, then send a separately authorized case | Each notification uses the saved preference effective for that send; historical artifacts do not silently relabel.                                |
| Explicit fallback      | Recipient has a missing/invalid legacy preference under a controlled fixture                                    | One documented fallback locale is used and logged; customer locale is not guessed as admin preference.                                            |
| Multiple recipients    | Two allowlisted administrators have different saved preferences                                                 | Each receives separately localized internal content/PDF with its own auditable provider result; no cross-recipient locale leakage.                |

## UAT-11 — original intake is missing from the customer dialogue context

- **Severity:** P2.
- **Status:** **IMPLEMENTED LOCALLY — RETEST REQUIRED** on the next immutable candidate.
- **Reproduction:** open TF-2 and compare the persisted lead message/type/contact/address/photo count with the dialogue area before the first outbound message.
- **Current stable behavior:** the operator sees one receipt but no prominent, clearly separate rendering of the customer's original submitted text and intake facts. Zero tracked follow-up questions can be phrased as if all questions were answered. `takvask` can fall through to the generic other-service label.
- **Exact local behavior awaiting deployment:** a compact expanded **Original inquiry** card precedes outbound history and preserves the lead message verbatim. It shows received time, localized public inquiry type, contact, original address fields and photo count without creating a `customer_question`. All six public inquiry enums (`takvask`, `takvask_impregnering`, `impregnering`, `takmaling`, `nytt_tak`, `usikker`) have NB/LT/EN labels. A zero total says **No follow-up questions tracked** (localized); **all answered** is reserved for `total > 0` with no unresolved questions.
- **Evidence:** E-CASE-02 and E-LOCAL-UX-02. Next-candidate proof must compare TF-2's stored exact message with rendered text, verify ordering before messages, all 18 service labels and the 0/resolved/unresolved question states.

## Next-candidate retest matrix

All rows must run against the next candidate's immutable URL and synthetic isolated data. Do not reuse the stable `hdl10bjlj` URL as proof for new code.

| State               | Required setup                                                                          | Recent case preview                                                                              | Full-history behavior                                                                                                                                               | Preservation assertion                                                                                                          | Viewport/focus evidence                                                                                                     |
| ------------------- | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| 0 history entries   | Synthetic case with no messages                                                         | Explicit empty state; no fake status, empty card stack or older control                          | Total is 0; search/filter controls either remain useful and disabled clearly or are omitted consistently                                                            | Explicit localized “No follow-up questions tracked”; never “all answered” and no inferred customer contact                      | 360/375, 768 and desktop; case tabs and following commercial section visible without a scroll trap                          |
| 1 history entry     | One inbound or outbound synthetic message                                               | One correctly labelled item with readable timestamp and direction                                | Total is 1; no pagination control                                                                                                                                   | If the item is an unanswered inbound question, it is visibly preserved                                                          | Open/close details by keyboard; focus remains on the message summary                                                        |
| 27 history entries  | Existing TF-1-equivalent distribution with at least one old unanswered inbound question | Exactly the five newest loaded messages; must not render all 25 default API rows in the overview | Use the explicit older action to load 27; search/category/status filters then cover the loaded 27 without duplicate IDs                                             | Oldest unanswered question remains in the separate outstanding list outside the newest slice and is not duplicated there        | Capture overview before full history, hash switch, older-page completion, return state and sticky offsets at mobile/desktop |
| 100 history entries | Synthetic-only high-volume case spanning categories, directions and statuses            | Same five-message overview limit and stable case layout as the 27-entry case                     | Bounded explicit older pagination and deterministic sort; filters apply to the currently loaded set; load all pages before asserting totals and boundary uniqueness | All unanswered items remain available in the separate list with deterministic oldest-first ordering until individually resolved | Keyboard-only pagination and filter reset; one document scrollbar; no header/tab overlap at all widths                      |

Cross-cutting next-candidate checks:

1. Record exact SHA, deployment ID, immutable URL, role, flags and synthetic case ID.
2. Verify login, queue, full case, back-state restoration and full reload without 5xx.
3. Distinguish in-page **Show details** from **Open case** in both label and URL.
4. Capture read-only, Preview-test, executable and missing-data states without performing customer-facing sends.
5. Archive screenshots under a repository evidence directory and record SHA-256 hashes; do not cite only clipboard or Temp paths.

### Isolated Preview deployment gate

Run only from a clean, committed branch for the isolated Vercel project. This is the complete approved command; it has no `--prod`, promotion or alias mutation:

```powershell
npx --no-install vercel deploy --target=preview --build-env DATABASE_URL_MIGRATE= --build-env PREVIEW_BASE_DATABASE_ENDPOINT_SHA256=c09b85f7a4792458554f96177d4caba345a0cebeb3efcc03d54954fe851a314e --build-env PREVIEW_EXPECTED_NEON_PROJECT_ID=ancient-paper-21872391
```

The empty `DATABASE_URL_MIGRATE` value is a temporary build-step override for this deployment only. The unrecoverable Sensitive Project Preview entry remains stored for recovery. The other two values are non-secret build controls and are intentionally supplied per deployment rather than added as permanent Project environment variables.

Accept the resulting candidate only when all of the following hold:

1. The build log contains `Preview database branch verified (<16-character fingerprint>)` before migrations.
2. The authenticated admin-only `/api/admin/platform-health` response reports the same `previewDatabaseTarget.endpointFingerprint` at runtime.
3. The response includes the expected-project fingerprint, exposes no database URL/credentials and has `Cache-Control: no-store`.
4. The prior stable immutable Preview remains READY and Production aliases, DNS, database and TF-2 remain unchanged.

## Owner post-submit stop on the current stable candidate

The controlled intake has now been submitted once. The resulting read-only case is:

`https://landing-hdl10bjlj-darbasnorvegija4-8212s-projects.vercel.app/admin-next-preview/cases/TF-2`

Continue only with read-only inspection. Do not press a delivery/retry control, run job ID `1`, create a quote, prepare a commercial package or promote the free-text address/area into RF. Before any later RF action, verify the canonical address and coordinates independently.

Current safety state:

- one owner-authorized controlled submission exists as lead `2` / case `TF-2`;
- the exact recipient remains `fornyelsegruppen@gmail.com`;
- receipt message `28` is provider-accepted/application-state ambiguous and must not be labelled delivered or unsent;
- the owner inbox contains the distinct customer receipt and internal notification, but this does not reconcile message `28` or authorise another send;
- no retry or execution of pending operational job `1` is authorised;
- no AI/Gemini job, draft or message was created;
- the free-text address and approximate area are unverified and cannot be used for RF, measurement, price, time or schedule;
- no Resend/provider-account change has been made;
- no Production database, deployment, alias or DNS mutation has been made;
- outbound Preview delivery remains guarded by the exact-recipient allowlist and `[PREVIEW TEST]` subject branding, but inherited provider-account isolation is not claimed.

## Benchmark principles retained

- Jobber: keep the real progression from request through quote, work, invoice and payment visible without inventing mandatory stages.
- Housecall Pro: use one central work/pipeline location with compact filters instead of another navigation shell.
- HubSpot: open the record by human identity, keep context and activity together, and collapse secondary technical detail.

The design adapts these information-architecture principles to Takfornyelse's canonical state engine and permission model. It does not copy another product, grant unsupported capabilities or create evidence for actions that were not performed.
